
// DistanceTime is a struct to hold the time of a runner for a distance
export interface DistanceTime {
    hours: number;
    minutes: number;
    seconds: number;
}

// Pace is a struct to hold the pace of a runner
export interface Pace {
    minutes: number,
    seconds: number
}
