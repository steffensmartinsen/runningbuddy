import React from 'react';

const TimeHandler = () => {
    return (
        <>
            <h1>TimeHandler</h1>
            <p>TimeHandler is the function that servers the /pace-calculator/time endpoint. It only accepts POST requests.</p>
        </>
    )
}

export default TimeHandler;