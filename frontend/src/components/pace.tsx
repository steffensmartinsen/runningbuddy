import React from 'react';

const PaceHandler = () => {
    return (
        <>
            <h1>PaceHandler</h1>
            <p>PaceHandler is the function that servers the /pace-calculator/pace endpoint. It only accepts POST requests.</p>
        </>
    )
}

export default PaceHandler;